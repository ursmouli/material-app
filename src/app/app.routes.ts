import { Routes } from '@angular/router';
import { SideNavigationComponent } from './side-navigation/side-navigation.component';

export const routes: Routes = [
    { path: 'slide', component: SideNavigationComponent }
];
